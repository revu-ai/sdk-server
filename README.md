# REVU Server SDK

`@revu-ai/server`: server-side crawler capture for REVU. Reports the crawler requests that reach a site's own server (including crawlers that never run JavaScript) to REVU.

Work in progress.
